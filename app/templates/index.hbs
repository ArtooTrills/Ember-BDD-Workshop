<section class="listing">
<div class="search">
    <label for="search__input>">Search by album or artist</label>
    {{input type="text" name="search__input" class="search__input" value=search }}
</div>
{{#each filteredAlbums as |album|}}
  {{fancy-album album=album}}
{{/each}}
</section>
